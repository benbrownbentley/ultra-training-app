// "Vert" is a working name. The brand will change before v2 public
// launch. Reference BRAND_NAME everywhere the app shows its name in
// user-facing surfaces. When the rename happens, update this file and
// the change propagates throughout the app.
//
// Internal identifiers (filenames, component names, CSS classes like
// `.vert-pulse-dot`) intentionally don't use this constant — they're
// not user-visible.

export const BRAND_NAME = "Vert";
export const BRAND_TAGLINE = "Adaptive training for endurance athletes";
