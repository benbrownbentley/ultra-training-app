## Summary

Phase 2 of the 5-phase plan (per `PROJECT_BRIEF.md` → Phase plan). Full design: [`PHASE_2_SPEC.md`](./PHASE_2_SPEC.md).

- Replaces the free-text `workouts.details` column with a kind-discriminated `planned_detail` JSONB (mirroring the `actual_detail` precedent from migration 013) and adds a Claude-generated per-workout `why` field (≤500 chars).
- Adds `workouts.source` (`manual` / `device`) as a schema-only seam for v3+ device-sync work — no v2 behavior reads it yet.
- Cuts every consumer (Today card, WeekStrip, drill-down, regen diff, history summariser, plan-derive metrics) over to read the structured payload directly. Regex parsers (`parseRunningSegments`, `parseStrengthExercises`, `parseRoutine`) and `STUB_WHY` lookups deleted. Legacy backfilled rows (`{ notes }`) render as minimal cards until the user regenerates.
- Single migration handles columns + backfill + drop + RPC update transactionally. One PR, one deploy. (See spec §3.5.)

## What changed

**Schema (`supabase/migrations/20260525000017_planned_detail.sql`)** — transactional: add `planned_detail jsonb` / `why text` / `source text check(source in ('manual','device'))`, backfill `{ notes: details }` for every row, drop `details`, replace `commit_plan_preview` RPC to insert the new columns.

**Generation (`lib/claude.ts`)** — `PLAN_TOOL` is now a discriminated `oneOf` per kind with strict inner/outer `kind` matching. New "Structured output requirements" section in `SYSTEM_PROMPT`. `GeneratedWorkout` requires `planned_detail` + `why`. `LoggedWorkout` consumes structured data; `formatHistory` / `formatStrengthActuals` summarise from the new shape.

**Validation (`lib/plan-validation.ts`)** — `PlannedDetailSchema` (zod discriminated union), `WHY_MAX_CHARS = 500`, new error codes: `planned_detail_invalid` / `kind_mismatch` / `why_missing` / `why_too_long`. Hooks into the existing auto-retry-once mechanic via `validateGeneratedPlan` — retry shape unchanged.

**Renderer (`lib/workout-content.ts`, `app/_components/workout/extract-metrics.ts`)** — `deriveWorkoutContent` projects structured payloads per kind; legacy rows return a minimal `WorkoutContent` with `isLegacy: true`. Metric tiles derive from `planned_detail` totals.

**Consumers** — `app/workout/[id]/page.tsx`, `WorkoutCard.tsx`, `WeekStrip.tsx`, `TodayPageClient.tsx`, `lib/plan-derive.ts`, `lib/preview.ts` (new `summarisePlannedDetailForDiff`), `lib/supabase/server.ts` (column list + row types).

**Actions (`app/actions.ts`)** — `attachPlannedExercises` deleted (formatStrengthActuals reads `planned_detail.exercises` directly). `addCustomActivity` writes a legacy-shape `{ notes }` planned_detail (extending the sheet UI to capture structure is deferred per spec §6 step 16). `previewPlan` / `submitWizard` pass `planned_detail` + `why` through the RPC.

**Tests** — 121/121 passing (+3 net). New: per-kind happy/failure validation, `why` cap, kind discriminator, legacy-row renderer; updated: claude / plan-derive / workout-content fixtures use structured payloads.

## Test plan

- [x] `npm run lint` — zero errors
- [x] `npm run test` — 121/121 passing
- [x] `npm run build` — completes successfully
- [ ] Smoke test post-deploy: wizard → plan generation → drill-down each kind → regen sheet → preview → accept

## Deploy steps (operational — for Ben after merge)

1. **Apply the migration on production Supabase** — `supabase/migrations/20260525000017_planned_detail.sql`. Either via the Supabase SQL editor or `npx supabase db push` (whichever is wired). The migration is transactional and idempotent on the column adds.
2. **Open the app immediately after the migration runs.** Between the migration and the first regen, every existing pending workout renders from the `{ notes: <original details> }` backfill — readable but minimal. This window is expected (per spec §3.5).
3. **Hit Regenerate on the regen sheet.** This fills `planned_detail` with full structured payloads for all future-dated rows.
4. **Spot-check one workout of each kind** (run / gym / hike / cross / physio / mobility) on the drill-down page to confirm structured rendering.

## Notes

- Source-field activation, GIN indexes, per-type column promotion, historical backfill, and prescribed-vs-actual deltas are all deferred (spec §8 / `PROJECT_BRIEF.md` → Phase 5+).
- The auto-retry-once retry shape is unchanged — only the validator was extended.
- Pre-existing local edits to `PROJECT_BRIEF.md` / `VERT_DESIGN_BRIEF.md` and untracked logo files were deliberately left out of this PR.
- Suggested next step before Phase 3: run a code review in Cowork (`"run a code review on the codebase"`) while the change is fresh — this PR touches the data layer, the LLM contract, and every renderer.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
